# Intermediate localization-pipeline artifacts — regenerated by tools/i18n
# (lupdate + the translation step). The committed sources of truth are the
# crater_<code>.ts catalogs; the committed runtime catalogs are the
# crater_<code>.qm the app bundles. Everything below is disposable scaffolding.
crater_template.ts
_sources.json
_map_*.json
